我有一個 React + TypeScript + Vite 的庫洛牌占卜專案，在嘗試部署時遇到建置錯誤。

**核心問題**：
- 開發環境 (`npm run dev`) 運行正常
- 建置時 (`npm run build`) 失敗，錯誤訊息：`"jsx" is not exported by "node_modules/react/jsx-runtime.js"`
- 這個錯誤阻擋了專案部署

**專案技術棧**：
- React 18.3.1 + TypeScript
- Vite 5.4.2 作為建置工具
- Tailwind CSS 3.4.1
- Supabase (資料庫 + 認證)
- OpenAI API (占卜解讀功能)

**我的目標**：
修復建置錯誤，讓 `npm run build` 能夠成功執行，以便部署專案。

**已嘗試的方法**：
1. 刪除 node_modules 重新安裝
2. 清理 npm cache
3. 檢查 package.json 依賴版本
4. 問題仍然存在

**請幫我**：
1. 分析可能的原因
2. 提供具體的修復步驟
3. 確保修復後 `npm run build` 能成功執行
4. 如果需要，可以調整 React、Vite 或 TypeScript 的版本和設定

**重要提醒**：
- 我需要保持專案的核心功能不變
- 環境變數檔案 (.env) 我會自己處理
- 主要目標是解決建置問題，不是功能開發

請提供詳細的修復步驟，謝謝！
```

## 📋 使用說明

### 步驟一：準備專案檔案
1. 解壓縮你從 Bolt 下載的檔案
2. 確保有完整的專案結構

### 步驟二：選擇 AI 平台
推薦平台：
- ChatGPT (GPT-4)
- Claude
- GitHub Copilot Chat
- Perplexity

### 步驟三：上傳檔案並提問
1. **上傳專案檔案**（如果平台支援）
2. **複製貼上上面的提示詞**
3. **等待 AI 分析並提供解決方案**

### 步驟四：如果需要更多資訊
如果 AI 需要更多細節，你可以補充：

```
補充資訊：
- 作業系統：Windows
- Node.js 版本：[你的版本]
- npm 版本：[你的版本]
- 錯誤發生在建置階段，具體是 JSX 轉換時失敗
- 專案在開發時完全正常，只有建置時出錯
```

## 🎯 預期結果

AI 應該會提供類似這樣的解決方案：
1. 檢查並修復 React 版本相容性
2. 調整 Vite 設定檔
3. 更新 TypeScript 設定
4. 提供具體的命令步驟